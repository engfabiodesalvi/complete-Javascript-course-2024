// Tipos de referência
// objects, arrays e functions


// Objeto -> reúne as infomações me um lugar só

// Declarando um objeto
let pessoa = {
    // key value pair
    nome: 'Rafael',
    idade: 25,
    estaAproado: true,
    sobrenome: 'de Souza',
    altura: 1.85,
    estadoCivil: 'casado',
    cor: 'branca'
}

// Acessando o valor de uma chave

console.log(pessoa);
